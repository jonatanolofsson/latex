TOP = $(TUP_CWD)
!python = |> python -B %f |>
!latex = |> rubber --pdf %f > /dev/null; eval "touch %B.{aux,log,out,bbl,blg}; exit $?" |> %B.pdf | %B.aux %B.log %B.out %B.bbl %B.blg
!ps2pdf = |> ps2pdf %f |> %B.pdf
.gitignore
